{{ 'style.featured-products.css' | asset_url | stylesheet_tag }}

{% if product.metafields.accentuate.you_might_also_like %}
    {% assign selected_handles = product.metafields.accentuate.you_might_also_like | split: '|' %}
    <h2 class="product__heading">You might also like</h2>
    <div class="products">
        <div class="products__container">
            {% for product in selected_handles %}
            <!-- <h3 class="product__title">{{ all_products[product].title }}</h3> -->
            <!-- <div class="products__wrapper"> -->
                <div class="product__item" id="shield">
                        <div class="product__image--wrapper">
                            <img class="product__image" src="{{ all_products[product].featured_image | img_url: '250x386' }}" alt="{{ all_products[product].featured_image }}">
                        </div>
                        <div class="product__content">
                            <h3>{{ all_products[product].title }}</h3>
                        <h5 class="product__money" >{{ all_products[product].price | money }}</h5>
                        <a class="product__link" href="{{ all_products[product].url }}">
                            <!-- {{ all_products[product].title }} -->
                            New Collection
                        </a>
                        </div>
                    </div>
            <!-- </div> -->
            {% endfor %}
        </div>
    </div>
{% endif %}


<style>
    .product__heading{
        width: auto;
        margin-top: 60px;
    }
    .products{
        /* width: 100%; */
        margin: 0 auto;
        padding: 44px 100px 80px 100px;
    }
    .products__container{
        display: grid;
        grid-gap: 40px;
        grid-template-columns: repeat(4, 1fr);
    }
    .product__item{
        background-image: url('../assets/svg.product-tile.svg');
        background-repeat: no-repeat;
        display: flex;
        align-items: center;
        justify-content: center;
        /* width: 256px; */
        height: 386px;
        position: relative;
        margin: 0 !important;
    }
    .product__image{
        position: relative;
        overflow: hidden;
        border-top-left-radius: 50%;
        border-top-right-radius: 50%;
        margin-top: 10%;
        width: 100%;
        height: 100%;
        /* object-fit: cover; */
    }
    .product__image--wrapper{
        height: 100%;
    }
    /* .product__image--wrapper {
    position: absolute;
    bottom: 80px;
    max-width: 200px;
    width: 100%;
    overflow: hidden;
    height: 220px;
    display: flex;
    justify-content: center;
}

.product__img {
    width: 100%;
} */
    .product__content{
        position: absolute;
        bottom: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
    }
    .product__link{
        margin: 0 0 12px 0 !important;
    }
</style>